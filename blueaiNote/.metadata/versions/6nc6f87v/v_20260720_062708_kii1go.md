---
key: 6nc6f87v
---

# 2026-07-17

&nbsp;

### discord 上线记录

```markdown
一、数据库 DDL(最关键,决定去重防线是否生效)
执行 campaign 作品提交唯一约束:logs/migrations/20260716_add_campaign_submission_unique_constraint.sql
该文件被 gitignore、不随代码部署,必须手动在生产库执行
内含两步:先 DELETE 历史重复记录(每组 (campaign_id, discord_user_id) 保留 MIN(id)),再 ADD CONSTRAINT uq_dc_submission_campaign_user
核对另外两个模型已定义、但既有生产表可能缺失的约束(create_all 不会给已存在的表补约束):
成员表 DiscordMemberSignup → uq_guild_member (guild_id, member_id)
服务器绑定表 DiscordGuildBinding → uq_guild_binding_guild_id (guild_id)(一对一收敛)
若为既有表且无此约束:需先清理历史重复数据(重复成员、重复 guild 绑定)再 ALTER ADD CONSTRAINT;目前 logs/migrations 仅提供了 campaign 一份 DDL,这两个约束若涉及存量表需另行补 DDL
执行前备份 + 低峰期操作:DELETE + ALTER 会锁表;每份 DDL 附带了回滚语句(DROP CONSTRAINT IF EXISTS)
二、配置与环境变量
DISCORD_GUILDS_API_TOKEN:guild 查询接口的内部鉴权 token(OAuth 修复新引入),生产必须配置,否则接口鉴权失败
多 guild 共享同一飞书表:每个 guild 必须配置 field_guild_id,否则 config.py:608 校验会直接阻止启动(正确防御,但需提前配好)
OAuth 回调:确认 client secret / 回调 URL 与 Redis 可用(state 一次性 nonce 依赖 Redis)
三、Redis(队列/锁跨实例生效前提)
确认生产 Redis 版本支持所用命令(SET NX EX、Lua 原子续期)——提交明确标注"生产 Redis 版本 Not-tested"
多实例部署须共享同一 Redis:飞书写入队列、lock_member、lock_startup_sync、OAuth nonce 均靠 Redis 跨实例协调(key 前缀 RkDiscordFeishu),否则串行/去重/单次初始化保证失效
四、真实环境冒烟验证(全部 Not-tested,上线后重点观察)
网关重连后初始化只执行一次(命令同步/View 注册/置顶/启动同步不重复)
飞书写入队列端到端:失败重入队、锁冲突退避、同成员更新不被覆盖
并发场景:同一用户重复入驻/重复提交作品只落一条(依赖第 1、2 步 DDL 已生效)
OAuth 完整绑定链路;大服务器批量身份组限速正常
一句话:代码已就绪,上线成败主要卡在第 1~2 步 DDL 必须人工执行且历史重复数据先清理,以及第 4~5 项配置齐全。这两类做不到,并发去重与多 guild 隔离在生产就是空防线。


===
requirement
【原始需求】全面分析 Discord 相关代码（src/bots/discord 全模块），查看是否存在逻辑漏洞并修复至健壮状态。

【本轮已验证——漏洞已全部修复】 原审查识别出的 4 项严重 + 若干高/中/低级逻辑漏洞，本轮验证确认已通过 12 个针对性提交全部修复：

严重：飞书写入队列消费可靠性(1cdb9892f)、网关重连重复初始化(5ce831c6a, bot.py _ready_done)、活动作品并发重复提交(3f5618463, ON CONFLICT+唯一约束)、OAuth 绑定链路安全(3166e5114, Redis nonce+DISCORD_GUILDS_API_TOKEN/team_id 鉴权)
高/中/低：入驻重复分配身份组(28bfab412)、活动跨服务器配置串用(a3aa5d508)、表单字段处理(3fc616bf8)、交互异常反馈(6e0bd06eb)、身份组状态不一致(df87bbbb4)、同步/标签/服务器绑定(5357dd33c)、配置失败与Embed超限(173d7a97c)、次要健壮性(1bef95b57) 验证证据：models_discord.py 已定义全部唯一约束(uq_dc_submission_campaign_user / uq_guild_member / uq_guild_binding_guild_id)；bot.py _ready_done、campaign/member ON CONFLICT、config.py:608 field_guild_id 启动校验均已落地；75 个 Discord 单元测试全部通过。
【结论】代码层面需求已实现；剩余为上线部署依赖（见 remark 上线 checklist）与真实环境验证，无待办代码任务。

remark
【Discord 修复上线 checklist——代码已就绪，上线成败卡在以下运维/配置步骤】

A. 数据库 DDL（最关键，决定去重防线是否生效）

手动执行 logs/migrations/20260716_add_campaign_submission_unique_constraint.sql（该目录被 gitignore、不随代码部署）；内含先 DELETE 历史重复（每组 campaign_id+discord_user_id 保留 MIN(id)）再 ADD CONSTRAINT uq_dc_submission_campaign_user。
核对存量表是否缺失另两个模型已定义的约束（create_all 不会给既有表补约束）：成员表 uq_guild_member(guild_id,member_id)、服务器绑定表 uq_guild_binding_guild_id(guild_id)。既有表无约束时需先清理重复（重复成员/重复绑定）再 ALTER；目前 logs/migrations 仅有 campaign 一份 DDL，这两个约束若涉及存量表需另行补 DDL。
执行前备份 + 低峰期（DELETE+ALTER 会锁表）；每份 DDL 附回滚语句。
B. 配置/环境变量 4. DISCORD_GUILDS_API_TOKEN：guild 查询接口内部鉴权 token，生产必配。 5. 多 guild 共享同一飞书表：每个 guild 必须配 field_guild_id，否则 config.py:608 校验直接阻止启动。 6. OAuth：client secret/回调 URL + Redis 可用（state 一次性 nonce 依赖 Redis）。

C. Redis（队列/锁跨实例生效前提） 7. 确认生产 Redis 版本支持 SET NX EX 与原子锁续期（提交标注生产 Redis 版本 Not-tested）。 8. 多实例必须共享同一 Redis：飞书写入队列、lock_member、lock_startup_sync、OAuth nonce 均靠 Redis 跨实例协调（key 前缀 RkDiscordFeishu）。

D. 真实环境冒烟验证（均 Not-tested，上线后重点观察） 9. 网关重连后初始化只执行一次；10. 飞书写入队列失败重入队/退避/不覆盖；11. 并发重复入驻/重复提交只落一条（依赖 A 步 DDL 已生效）；12. OAuth 完整链路 + 大服务器批量身份组限速。



```

&nbsp;

```plaintext
https://adharness-dev.domob-inc.cn/home?session_id=e1e71a60-bb2b-4049-b6ef-52c8e8d7859e

https://adharness-dev.domob-inc.cn/home?session_id=675a491b-95b6-4e68-a633-a030688328b6
```

&nbsp;